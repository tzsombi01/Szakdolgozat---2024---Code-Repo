server.port:8081
spring.data.mongodb.uri=mongodb://localhost:27017/project-management

cors.allowed-origins=${ALLOWED_ORIGINS:http:localhost:4200}

github.api.url=https://api.github.com/repos

email.username=${EMAIL_USERNAME:base}
email.password=${EMAIL_PASSWORD:basepw}

frontend.base.url=${FRONTEND_BASE_URL:http://localhost:4200}